package com.selldok.toy.employee.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * @author DongSeok, Kim
 */
@Controller
@RestController
@RequestMapping("employees")
public class AppliedStatusController {

	/*
	 * @GetMapping("{id}")
	 * 
	 * @ResponseBody public ResponseEntity<Employee> get(@PathVariable("id") Long
	 * id) { return new ResponseEntity(employeeService.get(id), HttpStatus.OK); }
	 */

}
